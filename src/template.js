// import { functionName, functionName2 } from "./utils.js"

/** @param {NS} ns **/
export async function main(ns) {

    // initalize arguments
    var myVar = ns.args[0];
    if (myVar === undefined) {
        myVar = "default";
    }
}